<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.animal.mapper.boardMapper">
	<!-- 동물 소개 글쓰기 등록 -->
	<insert id="information_writing">
		<selectKey keyProperty="bno" order="BEFORE" resultType="int">
			<!-- select 값을 keyProperty="bno"에 추가BEFORE로 먼저 -->
			select ifnull(MAX(ino)+1,1)from
			information
		</selectKey>
		insert into information(ino,
		animal_name,habitat,look,life,habit,food,enemy,i_type,main_img,sub_img,id)
		values(#{ino},#{animal_name},#{habitat},#{look},#{life},#{habit},#{food},#{enemy},#{i_type},#{main_img},#{sub_img},#{id}
	</insert>
</mapper>