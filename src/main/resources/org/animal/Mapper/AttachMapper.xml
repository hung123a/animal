<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.animal.mapper.AttachMapper">
	<!-- 파일경로, 파일명, uuid, image여부를 attach테이블 insert -->
	<insert id="info_insert">
		insert ignore into info_img(info_uid, info_name, info_image, info_upload,
		vision, info_no)
		value(#{info_uid},#{info_name}, #{info_image}, #{info_upload}, #{vision}, #{info_no})
	</insert>
</mapper>